% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flotsam.R
\name{ltsa}
\alias{ltsa}
\title{Local Tangent Space Alignment}
\usage{
ltsa(
  X,
  n_neighbors = NULL,
  ndim = 2,
  nn_method = "nnd",
  eig_method = "auto",
  eig_k = NULL,
  output = c("embedding", "result", "B"),
  include_B = FALSE,
  include_self = TRUE,
  normalize = FALSE,
  n_threads = 1,
  n_assembly_threads = 1,
  copy_max_mib = 256,
  verbose = FALSE,
  ...,
  assembly_max_mib = 4096
)
}
\arguments{
\item{X}{The input data matrix or data frame with one observation per row. If
a data frame is supplied, non-numeric columns are ignored. At least one
numeric column is required.}

\item{n_neighbors}{The size of local neighborhood (in terms of number of
neighboring sample points) used for manifold approximation. If \code{NULL}, the
default is \code{15} when neighbors are computed, or inferred when a precomputed
graph is supplied as \code{nn_method}. It must be at least \code{ndim + 2} so each
local projector has a residual direction beyond the constant and tangent
subspaces.}

\item{ndim}{The dimension of the space to embed into.}

\item{nn_method}{Method for finding nearest neighbors, or a precomputed
nearest-neighbor graph. Can be one of:
\itemize{
\item \code{"nnd"} Approximate nearest neighbors by Nearest Neighbor Descent.
\item \code{"exact"} Exact nearest neighbors by exhaustively comparing all items.
Slow for large datasets.
\item A precomputed nearest-neighbor index matrix.
\item A nearest-neighbor result object with an \code{idx} matrix.
}}

\item{eig_method}{How to carry out the final eigendecomposition. Possible
values are:
\itemize{
\item \code{"auto"} Use dense \code{\link[base:eigen]{base::eigen()}} when \code{n <= dense_n} or
\code{eig_k >= dense_fraction * n}, and otherwise use
\code{\link[RSpectra:eigs_sym]{RSpectra::eigs_sym()}}. This is the default.
\item \code{"rspectra"} Always use \code{\link[RSpectra:eigs_sym]{RSpectra::eigs_sym()}}. This method can
report hard backend convergence failures through RSpectra's \code{nconv}
metadata.
\item \code{"irlba"} Always use \code{\link[irlba:irlba]{irlba::irlba()}} and post-hoc residual
diagnostics.
\item \code{"svdr"} Always use \code{\link[irlba:svdr]{irlba::svdr()}} and post-hoc residual diagnostics.
\item \code{"eig"} or \code{"eigen"} Use the \code{\link[base:eigen]{base::eigen()}} function. This is only
feasible for small datasets and should be used for diagnostic purposes
only. Dense \code{"eig"} is the better diagnostic reference when algebraic
eigenvalue ordering matters.
}}

\item{eig_k}{Number of candidate vectors requested from the final
eigensolver. If \code{NULL}, the default is
\code{min(n - 1L, max(12L, ndim + 2L))}, where \code{n} is the number of
observations. Must satisfy \verb{ndim + 1 <= eig_k < n}. Larger values give
the Rayleigh-Ritz postprocessing a wider candidate span. Dense
eigenanalysis computes the full eigensystem, then retains the lowest
\code{eig_k} candidate vectors for that postprocessing.}

\item{output}{What to return:
\itemize{
\item \code{"embedding"} Return the embedding matrix. This is the default.
\item \code{"result"} Return a list containing the embedding, compact eigenanalysis
diagnostics, assembly diagnostics, and optionally \code{B}.
\item \code{"B"} Return the assembled unnormalized LTSA matrix and skip final
eigenanalysis.
}}

\item{include_B}{If \code{TRUE} and \code{output = "result"}, include the assembled
unnormalized LTSA matrix \code{B} in the result object. Ignored for other output
modes.}

\item{include_self}{Should an item be part of its own neighborhood? This has
a minor effect on most results, but work by Zhang and co-workers (2017)
suggests that this is in effect the main difference between LTSA and the
Hessian Locally Linear Embedding (HLLE) method, so setting this to \code{FALSE}
may allow emulating the HLLE method.}

\item{normalize}{If \code{TRUE}, use the normalized LTSA formulation described in
the "Normalized LTSA" section. The default is \code{FALSE}, which uses the
standard LTSA formulation.}

\item{n_threads}{Number of threads to use. Applies only to the nearest
neighbor calculation.}

\item{n_assembly_threads}{Number of threads to use when constructing the LTSA
alignment matrix \code{B} after nearest neighbors are computed. The default
\code{1} preserves the serial assembly path. Values greater than \code{1} opt into
parallel construction of \code{B}, which can be faster but may increase peak
memory use. See the
\href{https://jlmelville.github.io/flotsam/articles/numerical-diagnostics.html#threading}{threading note}
about avoiding oversubscription when BLAS is also multithreaded.}

\item{copy_max_mib}{Maximum size, in MiB, of the optional row-major dense
copy of \code{X} used during high-dimensional local Gram assembly. Set to \code{0}
to disable this copy.}

\item{verbose}{If \code{TRUE} log information about progress to the console.}

\item{...}{Optional eigensolver and diagnostic controls. See the
\verb{Eigensolver controls} section.}

\item{assembly_max_mib}{Maximum modeled assembly peak, in MiB. The default
\code{4096} permits moderate assembly workloads while rejecting or routing
multi-gigabyte staging plans before their large allocations. This cap is
separate from \code{copy_max_mib} and does not cover pre-existing inputs or the
later eigensolver.}
}
\description{
Apply the Local Tangent Space Alignment (LTSA) method (Zhang and Zha, 2004)
for dimensionality reduction.
}
\details{
\code{ltsa()} builds a symmetric positive-semidefinite LTSA alignment operator
\code{B} from local neighborhoods and returns its lowest nonconstant embedding
vectors. The constant vector is a known null direction of \code{B}, but the
nullspace can contain other directions. The default
\code{output = "embedding"} returns only the embedding matrix.

Use \code{output = "result"} to inspect compact diagnostics when convergence or
eigenspace ambiguity matters. The result contains the embedding,
eigenanalysis diagnostics, assembly diagnostics, and, when
\code{include_B = TRUE}, the assembled unnormalized matrix \code{B}.
\code{eigen$status} and \code{eigen$messages} summarize the solve; inspect those
condition-specific messages when \code{eigen$status} is not \code{"ok"}.

Use \code{output = "B"} to return the assembled unnormalized LTSA matrix and skip
final eigenanalysis.

When \code{output = "result"}, \code{eigen$method} records the requested policy or
canonical method name: automatic selection is stored as \code{"auto"}, and the
\code{"eigen"} alias is stored as \code{"eig"}. \code{eigen$backend$name} records the
backend actually used.
}
\section{Eigenanalysis and structural diagnostics}{

Rayleigh--Ritz postprocessing removes the known constant null direction from
the candidate span before selecting \code{ndim} coordinates. A larger observed
near-zero nonconstant eigenspace can make the individual coordinates
non-identifiable: \code{eigen$diagnostics$near_zero_block_truncated} is \code{TRUE}
when the requested embedding cuts through that observed block. The related
counts and boundaries describe only the returned candidate span; they do not
establish the full nullity of \code{B} or diagnose clusters.

\code{assembly$component_count}, \code{assembly$component_sizes}, and
\code{assembly$component_membership} describe connected components of the
co-membership graph induced by the effective neighborhoods used to assemble
\code{B}. Disconnected components imply componentwise constant null directions. A
connected graph does not rule out other weak or low-energy structure.
\code{assembly$component_embedding_overlap} reports how the standard LTSA
embedding subspace overlaps the nonconstant component-indicator contrast
subspace; it is not a graph-cut or clustering score. For normalized LTSA,
the corresponding field is
\code{eigen$normalized_details$component_embedding_overlap}.
}

\section{Normalized LTSA}{

\code{normalize = TRUE} applies symmetric Jacobi scaling to the assembled LTSA
alignment matrix \code{B}. Let \code{D = diag(B)}. The eigensolve is performed on
\verb{D^(-1/2) B D^(-1/2)}, and the selected vectors are mapped back with
\code{D^(-1/2)}. Equivalently, this solves the generalized eigenproblem
\verb{B v = lambda D v}.

This is a different generalized problem, not merely a better-conditioned
route to the standard LTSA eigenvectors. It keeps the alignment energy but
changes the orthogonality and centering constraints from the ordinary inner
product to the \code{D}-weighted inner product.

The mass \code{diag(B)} is residual leverage, not ordinary graph degree. Each
occurrence of a point in a neighborhood contributes the corresponding
diagonal of that neighborhood's residual projector, so the mass combines
reverse-neighborhood participation with local tangent leverage. Boundaries,
curvature, rank deficiency, and unusual local geometry can therefore affect
it. Mapping back from symmetric coordinates also amplifies points with small
mass.

With \code{output = "result"}, \code{eigen$normalized_details} contains \code{mass} and
\code{mass_summary}; the latter contains \code{quantiles}, \code{max_to_min_ratio},
\code{min_to_median_ratio}, \code{log10_max_to_min_ratio},
\code{log10_min_to_median_ratio}, \code{index_limit}, \code{smallest_mass_indices}, and
\code{largest_mass_indices}. The symmetric coordinates are stored in
\code{symmetric_embedding}, while the top-level \code{embedding} contains the mapped
generalized coordinates.

\code{generalized_absolute_residuals} contains the columnwise Euclidean norms of
\verb{B v - lambda D v}. \code{generalized_residual_scale} is
\code{sqrt(max(mass)) * max(eigen$lambda_max, 1)}, and \code{generalized_residuals} is
the absolute vector divided by that scale. \code{weighted_orthogonality_error},
\code{weighted_centering_error}, and \code{map_back_error} are the maximum absolute
entries of \verb{V' D V - I}, \verb{V' D 1}, and \verb{u - sqrt(D) v}, respectively.
\code{reverse_occurrence} contains \code{counts}, \code{quantiles}, and
\code{correlation_with_mass}; \code{component_embedding_overlap} contains the
normalized component-subspace comparison described above. Comparing
\code{embedding} with \code{symmetric_embedding} separates changes in the scaled
operator's eigenspace from pointwise map-back scaling. These fields do not
have ordinary normalized-graph-Laplacian or graph-cut interpretations.
}

\section{Precomputed neighbor input}{

\code{nn_method} may be a precomputed 1-based neighbor index matrix or an object
with an \code{idx} matrix. With \code{include_self = TRUE}, each row must include its
own row index and have \code{n_neighbors} columns. With \code{include_self = FALSE},
the first column must contain the row index and is dropped before LTSA
assembly, so the matrix must have \code{n_neighbors + 1} columns.
}

\section{Performance and memory controls}{

\code{copy_max_mib} limits an optional row-major dense copy of \code{X} used during
high-dimensional local Gram assembly. Increase it only when that copy is
useful and enough memory is available; set it to \code{0} to disable the copy.
\code{assembly_max_mib} is a separate cap on the modeled assembly peak. The
preflight includes the optional row-major copy when eligible, along with
route-specific staging, workspace, reduction, C++-to-R sparse and local-rank
conversion, sparse-output bounds, and post-assembly R payloads for rank and
component diagnostics, sparse-slot validation, and reverse occurrence. It
excludes the existing input, allocator overhead, thread stacks, library
internals, R object headers and diagnostic character-string contents,
workspaces internal to R primitives, and later eigensolver storage, so it is
not an exact peak-RSS prediction.

Sparse-output size depends on neighborhood overlap and numerical
cancellation. The preflight therefore labels the full compact staging and
representable final \code{dgCMatrix} output as separate conservative slot bounds.
When requested parallel assembly exceeds \code{assembly_max_mib}, \code{ltsa()} warns
and falls back to serial only if the serial bound is within the cap;
otherwise it fails before assembly staging is allocated. With
\code{output = "result"}, \code{assembly$memory} reports the requested and selected
routes, cap, relevant \code{sizeof} values, and component and phase bounds in
bytes.
\code{assembly$raw_entries_estimate} remains the triangular contribution count;
\code{assembly$raw_bytes_estimate} is present only for parallel assembly, where
raw integer-row and double-value staging buffers actually exist.

Local bases use a direct SVD when \code{ncol(X)} is no larger than the effective
neighborhood size and an eigendecomposition of the centered Gram matrix
otherwise. The Gram route applies a more conservative effective
singular-value threshold because forming the Gram matrix squares the
condition number. This stability tradeoff can produce different numerical
ranks near the threshold. With \code{output = "result"}, inspect
\code{assembly$local_solver_route}, \code{assembly$local_rank_histogram}, and
\code{assembly$rank_deficient_neighborhood_indices} for the route and its
observed ranks.
}

\section{Eigensolver controls}{

Automatic-selection controls:
\itemize{
\item \code{"auto"}: \code{dense_n} and \code{dense_fraction}. Dense eigenanalysis is selected
when \code{n <= dense_n} or \code{eig_k >= dense_fraction * n}; their defaults are
\code{100} and \code{0.5}.
}

Backend controls for explicit iterative methods:
\itemize{
\item \code{"rspectra"}: \code{tol}, \code{maxitr}, and \code{ncv}. See
\link[RSpectra:eigs_sym]{RSpectra::eigs_sym()} for their meanings. The
package default for \code{tol} is \code{1e-6}, deliberately looser than RSpectra's
own \code{1e-10} default; specify \code{tol} for a like-for-like comparison.
\item \code{"irlba"}: \code{tol}, \code{maxit}, and \code{reorth}.
See \link[irlba:irlba]{irlba::irlba()} for their meanings.
\item \code{"svdr"}: \code{tol}, \code{it}, and \code{extra}. See
\link[irlba:svdr]{irlba::svdr()} for their meanings.
}

These are the backend controls exposed by \code{ltsa()}; other arguments accepted
by the underlying packages are not automatically available. Values for the
listed controls are passed to the selected backend unchanged.

\code{resid_tol} and \code{gap_tol} set diagnostic thresholds for candidate residuals
and the Ritz boundary gap. They default to \code{1e-5} and \code{1e-4}, respectively.
They are accepted for every eigensolver mode and do not affect backend
selection.
Dense \code{"eig"}/\code{"eigen"} accepts only these diagnostic controls.

\code{shift_eps} controls the positive shift margin used by iterative methods and
defaults to \code{1e-6}. It is accepted only with an explicit iterative method.
Backend controls are accepted only with their explicit backend. The
requested policy or canonical method is stored in \code{eigen$method}
(\code{"eigen"} becomes \code{"eig"}), while \code{eigen$backend$name} identifies the
backend actually used (\code{"dense_eigen"}, \code{"rspectra"}, \code{"irlba"}, or
\code{"svdr"}). \code{output = "B"} does not accept eigenanalysis controls in \code{...}.
}

\examples{
# The default return is the embedding matrix.
small_iris <- iris[1:75, 1:4]
iris_ltsa <- ltsa(
  small_iris,
  n_neighbors = 12,
  nn_method = "exact"
)

# Request compact eigenanalysis and assembly diagnostics.
iris_result <- ltsa(
  small_iris,
  n_neighbors = 12,
  nn_method = "exact",
  eig_k = 8,
  output = "result"
)
iris_result$eigen$status
iris_result$eigen$messages
iris_result$eigen$method
iris_result$eigen$backend$name

# Return the assembled unnormalized LTSA matrix.
iris_B <- ltsa(
  small_iris,
  n_neighbors = 12,
  nn_method = "exact",
  output = "B"
)
dim(iris_B)
}
\references{
Zhang, Z., & Zha, H. (2004).
Principal manifolds and nonlinear dimensionality reduction via tangent space alignment.
\emph{SIAM journal on scientific computing}, \emph{26}(1), 313-338.
\url{https://doi.org/10.1137/S1064827502419154}

Zhang, S., Ma, Z., & Tan, H. (2017).
On the Equivalence of HLLE and LTSA.
\emph{IEEE transactions on cybernetics}, \emph{48}(2), 742-753.
\url{https://doi.org/10.1109/TCYB.2017.2655338}
}
